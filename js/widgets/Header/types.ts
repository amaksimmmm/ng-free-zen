import { LangData } from '../../features/Lang/types';
import { NavIteamData } from '../../features/Navigation/types';

export type HeaderData = {
  langs: LangData[];
  navigation: NavIteamData[];
};

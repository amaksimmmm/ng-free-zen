import { TitleData } from "../../ui/Title/types";
import { InputData } from "../../features/Input/types";

export type ModalData = {
  title: TitleData[];
  input: InputData[];
  buttonText: string;
}
